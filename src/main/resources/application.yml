spring:
  application:
    name: security_test
  profiles:
    active: dev

  messages:
    basename: messages
    encoding: UTF-8

logging:
 level:
   root: WARN
   com.example.demo: INFO

  
  
  # mvc:    
  #   throw-exception-if-no-handler-found: true
  # resources:
  #   add-mappings: false
# server.error.whitelabel.enabled: true